# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
1. **報廢日期鎖定**：解決 `DisplayMode: ==DisplayMode.View` 語法錯誤，成功鎖定當天日期（`Today()`）防呆禁止手動修改。
2. **流水號單號自動編碼**：完成 `FormHeaderTitle` 自動依日期與當日筆數編碼（格式如：`20260914001`）。
3. **全表單深度重設清空**：修正 `BtnReset` 與 `BtnNew`，一鍵徹底淨空連動變數（`locDrugRecord`、`locDrugCode`）與藥號、批號、數量、備註及原因，解決登入或新增時殘留範例值的問題。
4. **報廢原因選項調整**：將「其他原因」精準更名為「其他原因（請於備註說明）」。
5. **儲存並發送審核連動報廢紀錄清單**：
   - 解決微軟集合型態衝突（字串 vs 數值 `id: Value(varCurrentSerial)`），修復 `Collect` 寫入失敗問題。
   - 將左側靜態卡片升級為 `Gallery_History`（垂直資源庫），依 `id` 降冪排列，最新單號永遠排第一筆。
   - 實現三層式卡片呈現：第一層純單號（數值）、第二層藥號與藥名、第三層批號與數量。
6. **歷史紀錄 100% 完整原封還原**：點擊清單任一筆紀錄時，右側表單之藥號、藥名、日期、申請人、複核人、藥庫人員、批號、數量、報廢原因（`DefaultSelectedItems`）與備註全數原汁原味重現。
7. **申請人欄位解鎖**：將 `Input_Applicant` 的 `DisplayMode` 改為 `DisplayMode.Edit`，背景改為純白，開放藥師手動輸入或自訂。
8. **真正寫入 SharePoint**：已在 `BtnSubmit.OnSelect` 加上 `Patch('報廢網頁', Defaults(...), {...})`，寫入藥號、藥名、日期、批號、數量、原因、備註及審核狀態（固定 `"待審核"`）。
9. **（2026-09-17 完工）UI 緊湊美化與第 11 項附件格子可視化調整**：
   - 徹底解決播放模式下第 11 項附加檔案被視窗下緣截斷之問題。
   - 刪除殘留撐大捲軸的幽靈控制項 `ComboBox4`（原 Y=728）。
   - 23 個控制項 Y 座標緊湊重排，附件區底部上移至 650px，距螢幕底部狀態列維持 86px 巨大安全餘裕，全畫面文字與元件 100% 不超出螢幕。
10. **（2026-09-17 完工）打通 SharePoint 附件上傳通道與按鈕編譯錯誤清零**：
    - 定位出原按鈕點擊毫無反應之根本原因：微軟禁止直接對 SharePoint 原生 `{Attachments}` 執行常規 Patch，造成 4 個編譯錯誤鎖死按鈕。
    - 成功建立微軟原生表單 `Form_Attachment`（DataSource: `'報廢網頁'`，僅保留原生 `{Attachments}` 卡片，X=346, Y=540, W=950, H=110），取代舊有獨立控制項 `AttachmentsControl1`。
    - 按鈕公式升級為原子整合版：`Patch('報廢網頁', Defaults('報廢網頁'), { ... }, Form_Attachment.Updates); ResetForm(Form_Attachment);`。
    - 按鈕致命編譯錯誤徹底清零（0 Critical Errors），本機已備妥測試檔 `TEST20260917.pdf`（2,430 bytes）。
11. **（2026-09-18 完工）任務 1 ＋ 任務 3 驗收通過（PASSED）**：
    - **SharePoint 第一欄正名為「報廢單號」**：清單 `Title` 欄位成功重命名為「報廢單號」，Patch 公式第一位寫入流水號 `varCurrentSerial`。
    - **新增獨立「藥品代碼」欄位**：SharePoint 成功新增單行文字欄位「藥品代碼」，Patch 寫入 `Input_DrugCode.Text`。
    - **三位人員欄位轉為單行文字並全數寫入**：SharePoint 端「申請人」、「複核人」、「藥庫複核人員」已全部轉為單行文字（String），Patch 完整寫入三位人員之中文姓名與工號。
    - **App Checker 檢查通過**：0 Critical Errors, 0 High Errors。

31: 
32: ## 12. （2026-09-19 完工）任務 2 第一階段完工：清空假資料、真實 SharePoint 對接、審核狀態徽章、UX 重構與正式發布（PASSED）
33: - **徹底清除假資料**：移除 `App.OnStart` 與 `MainScreen1.OnVisible` 內寫死的 1001～1003 三筆假資料，改由 SharePoint 清單 `'報廢網頁'` 即時載入真實資料庫。
34: - **狀態徽章與動態變色**：左側 `Gallery_History` 卡片範本新增 `Badge_Status`（82×24px 圓角膠囊），依 `待審核`（橘底橘字）、`已核准`（綠底綠字）、`已退件`（紅底紅字）自動變色切換。
35: - **清單同步按鈕**：清單上方配置 `Btn_RefreshHistory`（圖示 `Icon.Reload`），隨時執行 `Refresh('報廢網頁')` 拉取雲端最新審核狀態。
36: - **新增申請單按鈕**：右上方新增 `Btn_NewForm`（「＋ 新增申請單」），點擊時一鍵深度淨空藥號、藥名、人員、批號、數量、原因、備註及附件框。
37: - **UX/UI 緊湊排版與附件體驗**：
38:   - 附件框加高至 150px 並整列寬度撐滿，「📎 附加檔案」連結直接可見不需向下捲動。
39:   - 白色表單框與全數欄位下移 16px，標題列加高至 38px 垂直置中，徹底解決送審按鈕卡框與排版過窄問題。
40: - **免打擾主管測試成功**：手動清空 SharePoint 舊資料後，送出正式單號 `20260919001`，清單刷新正常、附件上傳成功；後端 Teams 流程手動取消 3 筆舊審核並維持關閉狀態。
41: - **App Checker 零錯誤**：修復 `FontWeight`、`Input_Quantity.Default`、單號顯示等錯誤，達成 0 Critical / 0 High 錯誤。
42: - **雲端正式發布**：於 2026-09-19 09:51 完成線上正式 Publish。
43: - **本地原始碼同步**：透過 `canvasauthoring` MCP 成功執行 `connect` 與 `sync_canvas`，`cloud_app_sources/` 原始碼已 100% 同步回本機。
44: 
45: ## 🚦 目前狀態
46: 線上 Power Apps 應用程式（App ID: `d8ae7885-7892-4805-816a-a9593e587077`，名稱「報廢流程表1」）最新正式版已發布運行。本機原始碼（`cloud_app_sources/`）已完全同步最新版本。Teams 審核流程暫時保持關閉，準備進入「Teams 審核連動」階段。
47: 
48: ## ➡️ 下一步（即將執行）
49: 1. **【核心任務二第二階段】Power Automate 流程與 Teams 主管審核即時連動**：
50:    - 啟動並校準 Power Automate 審核推播流程（或依免打擾原則，先設定推播給測試者本人進行全鏈路實測）。
51:    - 驗證 Teams 自適應卡片收到新單推播（含單號 `20260919001` 或新單之藥碼、數量、原因與附件連結）。
52:    - 測試主管在 Teams 點選「✅ 核准」按鈕後，自動更新 SharePoint `'報廢網頁'` 之 `審核狀態` 為 `"已核准"`。
53:    - 回到 Power Apps 按下「🔄 重新整理」（或定時刷新），驗收左側清單徽章由橘色「待審核」自動跳轉為綠色「已核准」！
54: 
55: ## ⚠️ 注意事項
56: - 微軟 `canvasauthoring` MCP 支援雙向連線，本機 `cloud_app_sources` 為官方 YAML 規範。
57: - 微軟原生 `Attachments` 控制項受限於系統底層樣式，附件條目僅支援垂直列出，無法強制水平橫排。
58: - 目前 SharePoint 清單首欄已標準化為「報廢單號」，三位人員為單行文字。
59: - Power Automate 流程重新啟用時，請先確認審核人信箱（Approver Email）指向，避免誤發真實主管。
60: 
61: ## 🕐 最後更新
62: - 時間：2026-09-19 10:15
63: - 更新者：Google Antigravity (指揮官) @ 江瑞益的MacBook Air
64: - 狀態：任務二前端與真實 SharePoint 串接全部驗收通過，雲端原始碼已同步回本機，準備啟動 Teams 審核連動。
